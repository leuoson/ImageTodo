{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main ImageTodo window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
